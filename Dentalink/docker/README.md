# Docker

Carpeta reservada para Dockerfiles, configuraciones auxiliares y scripts relacionados con contenedores.

El archivo `docker-compose.yml` principal se mantiene en la raiz para facilitar el arranque local.
